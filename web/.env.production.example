# Ara Sales web admin — PRODUCTION build env.
# Vite inlines VITE_* at build time, so set this before `npm run build`.
# Point it at your deployed backend (Azure App Service) URL — no trailing slash.
VITE_API_BASE_URL=https://ara-sales-api.azurewebsites.net
