# Production: leave the API base EMPTY so the app calls relative "/api" on its own
# origin, and the host (Vercel) rewrites that to the real backend server-side
# (see web/vercel.json). This avoids CORS entirely — the browser only ever talks
# to the Vercel domain. Do NOT put the Azure URL here.
VITE_API_BASE_URL=
